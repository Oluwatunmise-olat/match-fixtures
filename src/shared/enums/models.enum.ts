export enum ModelNames {
	FIXTURES = 'fixtures',
	USERS = 'users',
	TEAMS = 'teams',
	PLAYERS = 'players',
}

export enum UserRoles {
	ADMIN = 'admin',
	NORMAL = 'normal',
}

export enum FixtureStatus {
	PENDING = 'pending',
	ONGOING = 'ongoing',
	COMPLETED = 'completed',
}
